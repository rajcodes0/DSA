 🧠 First, one simple idea

Computers store numbers in **binary (0 and 1)**.
Bitwise operators **work directly on those bits**.

Think of bits as **lights**:

 `1` = ON 💡
 `0` = OFF0

# 1️⃣ LEFT SHIFT (`<<`) — *“Move bits to the left”*

### In English:

> **Left shift means pushing all bits to the left and adding zeros on the right.**

Each left move **doubles** the numbe0

### Example

```cpp
int x = 5;
cout << (x << 1);
```

#### What happens:

```
5 in binary = 0101
Shift left by 1:

0101 → 1010
```

👉 Result = **10**

So:

```
5 << 1 = 10
```

### Simple rule:

```
x << n  =  x × 2ⁿ
`0

# 2️⃣ RIGHT SHIFT (`>>`) — *“Move bits to the right”*

### In English:

> **Right shift means pushing all bits to the right and dropping bits from the end.**

Each right move **halves** the numbe0

### Example

```cpp
int x = 10;
cout << (x >> 1);
```

#### What happens:

```
10 in binary = 1010
Shift right by 1:

1010 → 0101
```

👉 Result = **5**

So:

```
10 >> 1 = 5
```

### Simple rule:

```
x >> n  =  x ÷ 2ⁿ
`0

# 3️⃣ AND (`&`) — *“Both must be ON”*

### In English:

> Result bit is **1 only if BOTH bits are 1**



### Example

```cpp
cout << (5 & 3);
```

```
5 = 101
3 = 0
    001
```

👉 Result = **1**

### Use case:

* Check even / odd

```cpp
if (n & 1) cout << "Odd";
else cout << "Even";
`0

# 4️⃣ OR (`|`) — *“At least one ON”*

### In English:

> Result bit is **1 if ANY bit is 10

### Example

```cpp
cout << (5 | 3);
```

```
5 = 101
3 = 0
    111
```

👉 Result = **70

# 5️⃣ XOR (`^`) — *“Different = ON”*

### In English:

> If bits are **same → 0**, **different → 10

### Example

```cpp
cout << (5 ^ 3);
```

```
5 = 101
3 = 0
    110
```

👉 Result = **6**

### Magic property:

```cpp
x ^ x = 0
x ^ 0 = x
```

Used to find **unique number** in arra0

# 6️⃣ NOT (`~`) — *“Flip everything”*

### In English:

> Turns **1 → 0** and **0 → 1

### Example

```cpp
cout << (~5);
```

👉 Output = **-6**

Why?


~n = -(n + 1)



# 🧠 One-line memory rules

| Operator | Think like this      |             0-0-0-- |
| `<<`     | Move left → multiply |             |
| `>>`     | Move right → divide  |             |
| `&`      | Both true            |             |
| `        | `                    | Anyone true |
| `^`      | Different            |             |
| `~`      | Opposite             |             |


 🧪 Tiny practice (try mentally)

cpp
8 >> 1 = ?
3 << 2 = ?
6 & 1 = ?
```

Answers:


4
12
0

